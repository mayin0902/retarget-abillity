# Movie60 历史实验

这里保留 3.3 及更早 Movie60 校准、Agent、AIGC、报告和 Release 开发脚本，便于从 Git
历史与已发布资产复查证据。它们不是新开发者的日常入口，正式 `src/` 不得依赖本目录。

当前日常入口只看根目录 `scripts/` 和四份 `docs/`。需要重跑某个历史实验时，从仓库根目录
执行相应脚本，并先核对脚本引用的冻结 Run、授权素材、模型和费用边界。
